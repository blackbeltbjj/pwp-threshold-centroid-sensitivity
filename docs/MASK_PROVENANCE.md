# Pacific Mask Provenance

The analysis uses the fixed historical Pacific binary mask.

Provenance chain:

legacy 1° mask
? reconstructed 180 × 360 cell-centred grid
? exact 4 × 4 replication
? 720 × 1440 OISST-compatible grid

The mask is time invariant and common to all SST thresholds.

Frozen mask objects are stored under:

data/sample_or_small_derived/
